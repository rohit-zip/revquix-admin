/**
 * useAvailableWorkspaces — NOT used in revquix-admin.
 * The admin app has a single flat nav (no workspace switcher).
 * This stub is kept so copied components that import it don't break at compile time.
 */
export function useAvailableWorkspaces() {
  return []
}
